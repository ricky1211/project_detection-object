import React from 'react';
import ReactDOM from 'react-dom/client';
import DetectionToolkit from './DetectionToolkit.jsx';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <DetectionToolkit />
  </React.StrictMode>,
);